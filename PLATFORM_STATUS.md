# حالة منصة حكيم — ما اكتمل وما تبقّى

آخر تحديث: 15 سبتمبر 2026

حكيم سوق رعاية صحية لمصر فقط، عربي أولاً، مبني على Laravel. الهاتف هو المعرّف الأساسي للمريض. حسابات فريق المنصة تدخل من `/admin/login`. لا توجد بادئات `/ar` أو `/en` في الروابط؛ اللغة من الجلسة وRTL يتبعها.

---

## حسابات التجربة (محلي)

| الدور | الدخول | المعرّف | كلمة المرور |
| --- | --- | --- | --- |
| مدير المنصة | `/admin/login` | `admin@hakeem.test` أو `01000000000` | `password` |
| عيادة | `/login` | `01111111111` | `password` |

ملاحظة: رقم واتساب الدعم الافتراضي `01000000000` يصطدم برقم الأدمن. غيّره من إعدادات SEO/الدعم قبل التجربة الحية للدعم.

---

## ما اكتمل ويعمل

### الموقع العام
- الصفحة الرئيسية، الأطباء، العيادات، العروض، المختبرات، التخصصات، الخدمات، المدن، الصفحات التعريفية (كيف يعمل، من نحن، تواصل، شكاوى، مساعدة، شروط، خصوصية).
- بحث متعدد الكلمات (كل كلمة شرط AND) مع فلتر المحافظة **والمدينة** على الأطباء والعيادات والعروض والمختبرات.
- خريطة Leaflet لفروع الصفحة في `/clinics`، وخريطة كل الفروع ذات الإحداثيات في `/admin/map`.
- حجز موعد من ملف الطبيب، قائمة المواعيد، إلغاء عبر `BookingManager::transition` (تأكيد في الواجهة)، تقييم بعد زيارة مكتملة فقط.
- ملف المريض `/account` (الاسم، الميلاد، النوع، المدينة، اللغة، تفضيلات التنبيه) مع شريط «أكمل ملفك» حتى يكتمل الاسم + الهاتف المفعّل + المدينة.
- جرس تنبيهات داخل التطبيق مع صوت (WebAudio) في الموقع العام ولوحة الأدمن ولوحة العيادة؛ صفوف الصندوق تُحفظ دائماً حتى لو كانت SMS/FCM/واتساب تسجَّل في اللوج فقط.
- الشعار وfavicon والوصف وحسابات السوشيال من إعدادات SEO تظهر في الهيدر/الفوتر/أيقونة المتصفح عند ضبطها.
- وكيل الموقع Hermes للمريض (ردود مهيكلة + روابط حجز، بدون شحن بوابة دفع).

### لوحة تحكم المنصة
- بيانات مرجعية: محافظات، مدن، تخصصات، أنواع خدمات، خطط، أكواد خصم، إعدادات دفع، كتالوج تحاليل وباقات، عروض، صفحات SEO.
- **المستخدمون (المرضى):** تسجيل، تفعيل الهاتف، هل نزّل التطبيق، آخر دخول، الحجوزات، تفعيل/إيقاف.
- **العيادات:** توثيق، اشتراك، فروع، أطباء، حجوزات.
- **الفوترة:** إجمالي الاشتراكات، اشتراكات 30 يوماً، الحجوزات المدفوعة والمكتملة (بدون تحصيل بوابة بعد).
- **الحضور والانصراف:** بدء شفت / إنهاؤه / ساعات. الموظف يرى سجلّه؛ من لديه `manage_attendance` يرى الجميع.
- **تقارير الأخطاء:** استثناءات ≥ 500 مجمّعة حسب النوع/الملف/السطر، مع حلّ وإعادة فتح.
- **التحليلات:** مشاهدات، حجوزات 30 يوماً، العيادات/الأطباء/الخدمات الأكثر طلباً، والتحاليل الأكثر عرضاً من العيادات (حجز المختبر غير مربوط بعد).
- صلاحيات الموظفين حسب الدور: مدير المنصة `*`، موظف الدعم مقيّد (تذاكر، مستخدمون، عيادات، أخطاء، تحليلات — بلا خطط/موظفين/دفع). تظهر صلاحيات كل دور تحت الاختيار في نموذج الفريق.
- تدقيق، تذاكر دعم، تقييمات، إعدادات النظام (SMTP / Firebase / SMS / واتساب) مع probe.
- وكيل أدمن **مؤجَّل**: لا نموذج لغوي. يوجد `GET /api/v1/admin/overview` (صلاحية أدمن + تحليلات) يعيد لقطة JSON جاهزة لوسيط لاحق.

### لوحة العيادة
- ملف، فروع وساعات، أطباء، خدمات، عروض، تحاليل معروضة، اشتراك، استقبال، طابور اليوم (تأكيد/إلغاء/إكمال/إعادة جدولة)، حضور وانصراف لفريق العيادة.

### واجهات البرمجة الحالية
- عام: `/api/v1/status`, `/search`, `/reference/*`, `POST /agent/messages`.
- جوال (Sanctum): `POST /auth/login`, `GET/PUT /me`, `POST /auth/logout`, `GET /bookings`, `DELETE /bookings/{id}`, `POST /devices` (يسجّل تثبيت التطبيق), `GET/PUT /inbox`.
- أدمن: `GET /api/v1/admin/overview`.

### إشعارات
- المصفوفة قابلة للضبط من اللوحة. القنوات الخارجية تُسجَّل في اللوج إن نقصت المفاتيح. صندوق داخل التطبيق يُكتب للمريض ومالك/فريق العيادة وفريق المنصة على أحداث الحجز، مع رابط مناسب لكل دور.

---

## ما لم يكتمل (المطلوب لإغلاق المنصة)

مرتّب حسب الأثر. هذا هو ملف المتبقي.

### 1) المدفوعات والمحاسبة الحقيقية
- لا شحن لبوابة دفع. الحالة `paid` تُعلَّم يدوياً/داخلياً.
- لا فواتير إلكترونية، لا تسوية للعيادات، لا عمولة منصة محسوبة، لا تصدير محاسبي.
- كود الخصم على الاشتراك موجود؛ تطبيقه على حجز المريض غير مكتمل كرحلة دفع.

### 2) تفعيل الهاتف وOTP
- عمود `phone_verified_at` والشاشة الإدارية موجودان. لا إرسال OTP (SMS/واتساب/Firebase) ولا مسار المريض لتأكيد رقمه بنفسه. شريط اكتمال الملف يبقى حتى يفعّل الأدمن أو يُبنى OTP.

### 3) محرّك المواعيد (التعارض والسعة)
- الحجز يحفظ `scheduled_at` بدون منع التداخل أو احترام ساعات الفرع أو مدة الكشف. لا قائمة slots حقيقية.

### 4) ما بعد الحجز السريري
- لا روشتة، لا ملف طبي، لا نتائج تحاليل للمريض، لا تذكير مجدول بالموعد (الحدث موجود في المصفوفة فقط).
- جلسة التقييم النفسية تُعلَّم على أول زيارة؛ لا مسار متابعة سريري بعدها.

### 5) المختبرات
- كتالوج + سلة موجودة. لا إتمام طلب، لا اختيار فرع سحب، لا دفع، لا تتبع نتيجة. «الأكثر طلباً» حالياً = عدد العيادات التي تعرض التحليل وليست حجوزات.

### 6) وكيل Hermes (لوحة الأدمن)
- **متروك عمداً لاحقاً** كما طُلب. المتوفر اليوم: لقطة `/api/v1/admin/overview` بحالة `agent.status = planned`.
- المطلوب لاحقاً: محادثة أدمن بصلاحيات أدمن، قراءة اللقطة + أدوات (بحث مستخدم/عيادة، ملخص أخطاء، اقتراح خطوة تالية تشغيلية). لا نموذج توليدي مربوط الآن.

### 7) وكيل Hermes (المريض / الموقع)
- يعمل بردود مهيكلة وروابط. لا حجز مباشر من المحادثة (ما زال يوجّه لصفحة الحجز). لا ذاكرة طويلة، لا LLM خارجي إلزامي.

### 8) تطبيق الهاتف الأصلي
لا يوجد مشروع iOS/Android/React Native في هذا المستودع. الـ API الحالي يكفي لشريحة أولى. المتبقي للتطبيق:

| الشاشة / القدرة | الحالة في الـ API | المتبقي في التطبيق |
| --- | --- | --- |
| تسجيل دخول برقم الهاتف | جاهز | UI + تخزين التوكن الآمن |
| الملف الشخصي | `GET/PUT /me` | إكمال المدينة/التنبيهات |
| البحث أطباء/عيادات/عروض/تحاليل | `GET /search` + مراجع جغرافية | شاشات فلتر مدينة + كلمات |
| قائمة الحجوزات وإلغاء | جاهز | لا إنشاء حجز من الـ API بعد |
| إنشاء حجز | غير موجود | يحتاج `POST /api/v1/bookings` + slots |
| الصندوق + صوت | `GET/PUT /inbox` | FCM محلي + جرس في الخلفية |
| تسجيل الجهاز | `POST /devices` | طلب إذن الإشعارات وإرسال توكن FCM |
| OTP | غير موجود | بعد بناء الإرسال |
| الخريطة | غير موجودة في الـ API كمجموعة pins | يمكن مؤقتاً من نتائج البحث إن أُضيفت الإحداثيات |
| الدفع داخل التطبيق | غير موجود | بعد البوابة |
| المختبر | سلة ويب فقط | API طلبات تحاليل |
| Hermes داخل التطبيق | `POST /agent/messages` (عام، معدل) | شاشة محادثة؛ التوثيق اختياري |

بعد أول تثبيت، `app_installed_at` يظهر في لوحة المستخدمين.

### 9) Mobile API — الفجوات
- لا `POST /bookings` (إنشاء).
- لا `POST /auth/otp` / تحقق.
- لا موارد طبيب/عيادة تفصيلية غير البحث.
- لا رفع صور، لا سجل طبي، لا نتائج.
- لا قائمة pins للخريطة.
- الوكيل العام غير مقيّد بتوكن (معدل 20/دقيقة) وهذا مقصود للموقع؛ للتطبيق يفضّل مسار موثّق لاحقاً.

### 10) إرسال فعلي للتنبيهات
- Push/SMS/واتساب/بريد: لوج إذا غابت المفاتيح. لا Worker تذكير قبل الموعد. الجرس الحالي للمتصفح المفتوح (استطلاع 15 ثانية) وليس إشعار نظام iOS/Android.

### 11) صلاحيات أدق من الدور
- الصلاحيات مربوطة بالدور (مدير / دعم) وليست مصفوفة لكل موظف على حدة. يكفي للمرحلة الحالية؛ التفصيل لكل شاشة يحتاج جدولاً إضافياً.

### 12) أخرى
- لا تطبيق native ولا متجر.
- `APP_TIMEZONE` يبقى UTC (قرار قائم). العرض المحلي للمستخدم غير مطبّع على توقيت مصر في كل الشاشات.
- تقييمات حقيقية فقط بعد اكتمال زيارة — لا تقييمات وهمية في السيدرز.

---

## ملخص تنفيذي

| المجال | النسبة التقريبية | ملاحظة |
| --- | --- | --- |
| كتالوج وبحث وSEO وموقع عام | ~90% | ينقص slots ودفع ومختبر مكتمل |
| حجوزات العيادة + المريض ويب | ~75% | ينقص التعارض، التذكير، ما بعد الزيارة |
| لوحة الأدمن التشغيلية | ~80% | ينقص محاسبة حقيقية وOTP ووكيل أدمن |
| حضور الموظفين | ~85% | شفت واحد مفتوح؛ لا سياسات تأخير/إجازات |
| إشعارات | ~60% | صندوق + جرس جاهزان؛ الإرسال الخارجي لا |
| Mobile API | ~40% | دخول/ملف/قائمة/إلغاء/جهاز؛ لا إنشاء حجز |
| تطبيق الهاتف | 0% | يُبنى على الـ API |
| Hermes أدمن | 5% | لقطة JSON فقط |
| مدفوعات | ~15% | أوضاع دفع وإعدادات؛ لا تحصيل |

---

## توقّع الخطوة التالية (ترتيب مقترح)

1. **إنشاء الحجز من Mobile API + slots بسيطة** حتى يبدأ تطبيق الهاتف دون انتظار البوابة (الدفع في العيادة يبقى الافتراضي).
2. **OTP لتفعيل الهاتف** حتى يختفي شريط اكتمال الملف ويُعتمد الرقم كمعرّف موثوق.
3. **إتمام طلب المختبر** أو **تذكير الموعد المجدول** — أيهما أقرب لعرض تشغيلي على العيادات التجريبية.
4. **بوابة الدفع** بعد ثبات مسار الحجز (لا تُشحن الآن).
5. **تطبيق الهاتف (شريحة React Native)** على الـ API الحالي: دخول، بحث، مواعيدي، صندوق، تسجيل FCM.
6. **وكيل أدمن** بعد استقرار اللوحة؛ يستهلك `/api/v1/admin/overview` ثم أدوات قراءة فقط قبل أي كتابة.

ما لا يُنصح به كخطوة تالية: بناء LLM لأدمن قبل اكتمال الحجوزات والجوال، أو محاسبة كاملة قبل أول تحصيل حقيقي.

---

## أين تُضبط الأشياء

- هوية المنصة (شعار، favicon، وصف، سوشيال، روابط التطبيق): `/admin/seo/site`
- SMTP / Firebase / SMS / واتساب: `/admin/system`
- قنوات الإشعارات: `/admin/notification-settings`
- المستخدمون والعيادات والفواتير والخريطة والحضور والأخطاء: مجموعة «العملاء والعيادات» في الشريط الجانبي
