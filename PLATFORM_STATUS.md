# حالة منصة حكيم — ما اكتمل وما تبقّى

آخر تحديث: 23 سبتمبر 2026

حكيم سوق رعاية صحية لمصر فقط، عربي أولاً، مبني على Laravel. الهاتف هو المعرّف الأساسي للمريض. حسابات فريق المنصة تدخل من `/admin/login`. لا توجد بادئات `/ar` أو `/en` في الروابط؛ اللغة من الجلسة وRTL يتبعها. التوقيت المخزَّن يبقى UTC؛ العرض للمريض بتوقيت مصر (`Africa/Cairo`).

---

## حسابات التجربة (محلي)

| الدور | الدخول | المعرّف | كلمة المرور |
| --- | --- | --- | --- |
| مدير المنصة | `/admin/login` | `admin@hakeem.test` أو `01000000000` | `password` |
| عيادة | `/login` | `01111111111` | `password` |

ملاحظة: رقم واتساب الدعم الافتراضي `01000000000` يصطدم برقم الأدمن. غيّره من إعدادات SEO/الدعم قبل التجربة الحية للدعم.

---

## ما اكتمل ويعمل

### الموقع العام
- الصفحة الرئيسية (تدرج أزرق فاتح ناعم)، الأطباء، العيادات، العروض، المختبرات، التخصصات، الخدمات، المدن، المكتبة الطبية، الصفحات التعريفية — محتواها قابل للتعديل من `/admin/site-pages` مع سيو وخريطة موقع.
- بحث متعدد الكلمات (كل كلمة شرط AND) مع فلتر المحافظة **والمدينة**.
- صفحات تخصص×مدينة/محافظة، وخدمة×مدينة/محافظة، و`/home-care` و`/teleconsultation`.
- خريطة Leaflet لفروع `/clinics`، وخريطة الفروع ذات الإحداثيات في `/admin/map`.
- حجز موعد من ملف الطبيب مع **slots** من ساعات الفرع/الطبيب، منع التداخل، عنوان زيارة منزلية عند الحاجة، ووقت مخصّص.
- قائمة المواعيد بتوقيت مصر، إلغاء عبر `BookingManager::transition` (تأكيد في الواجهة)، تقييم بعد زيارة مكتملة فقط.
- جلسة فيديو: رمز غرفة عند الحجز الإلكتروني، وصفحة `/appointments/{id}/video` (تُفتح بعد تأكيد العيادة؛ الغرفة عبر Jitsi العام).
- ملف المريض `/account` مع OTP لتفعيل الهاتف وبريد التفعيل، شريط اكتمال الملف، الولاء، التأمين.
- السجل الطبي `/account/records` (استشارة، روشتة PDF، نتائج تحاليل) وتحقق عام `/verify/{code}`.
- سلة وتحاليل مع إتمام طلب، سحب منزلي مع دبوس خريطة، تتبع النتيجة لدى المريض والعيادة والأدمن.
- جرس تنبيهات داخل التطبيق مع صوت في الموقع العام ولوحة الأدمن ولوحة العيادة.
- شعار وfavicon ووصف وسوشيال من إعدادات SEO. بانر كوكيز محلي. Sitemap + schema.org.
- وكيل الموقع Hermes للمريض (ردود مهيكلة + روابط حجز/إلغاء، بدون شحن بوابة دفع).

### لوحة تحكم المنصة
- بيانات مرجعية: محافظات، مدن، تخصصات، أنواع خدمات، خطط، أكواد خصم، إعدادات دفع، كتالوج تحاليل وباقات، عروض، مقالات المكتبة، صفحات SEO، صفحات الموقع (CMS).
- المستخدمون والعيادات والأطباء والفواتير (حالة الدفع، نوع الاشتراك، تفاصيل الدفع، إرسال فاتورة/تذكير) والخريطة والحضور وتقارير الأخطاء والتحليلات والتقييمات والتذاكر والتدقيق.
- صلاحيات حسب الدور (مدير `*` / دعم مقيّد). وكيل أدمن **مؤجَّل**: `GET /api/v1/admin/overview` فقط.

### لوحة العيادة
- ملف، فروع وساعات، أطباء، خدمات، عروض، تحاليل، طلبات تحاليل ونتائج، روشتات بعد الزيارة، اشتراك، استقبال وطابور اليوم، حضور، دخول غرفة الفيديو للحجوزات الإلكترونية.

### واجهات البرمجة الحالية
- عام: `/api/v1/status`, `/search`, `/reference/*`, `GET doctors/{slug}`, `GET doctors/{slug}/slots`, `GET clinics/{slug}`, `GET map/pins`, `POST /agent/messages`.
- جوال (Sanctum): تسجيل/دخول، `GET/PUT /me`, `POST /me/phone/code|verify` و`POST /auth/otp` + `/auth/otp/verify`, `GET/POST/DELETE /bookings`, `GET /records`, `GET /lab-orders`, `POST /devices`, `GET/PUT /inbox`.
- أدمن: `GET /api/v1/admin/overview`.

### إشعارات
- المصفوفة قابلة للضبط من اللوحة. القنوات الخارجية تُسجَّل في اللوج إن نقصت المفاتيح.
- صندوق داخل التطبيق يُكتب على أحداث الحجز والتحاليل والروشتة.
- تذكير مجدول قبل الموعد بـ 24 ساعة: الأمر `bookings:send-reminders` كل ساعة (`withoutOverlapping`).

---

## نشر أول إصدار / إعادة بناء (Docker)

```bash
cd /var/hakeem
git pull
./setup.sh --resetup --domain eg.hakeem.com.sa --url https://eg.hakeem.com.sa
```

`--resetup` = `--fresh` + `--rebuild` (مسح volumes/قاعدة Docker، بناء صورة بدون كاش مع Node/npm/Vite، migrate + seed).

بدون Docker (PHP على المضيف بعد `git pull`):

```bash
composer install --no-dev --optimize-autoloader
npm ci && npm run build
php artisan migrate --force
HAKEEM_SEED_DEMO=true php artisan db:seed --force   # أول إطلاق فقط؛ ثم عطّل المتغير
php artisan optimize:clear
```

`ServiceTypeSeeder` يملأ أنواع الخدمات دائماً. الكتالوج التجريبي (عيادات/أطباء) يعمل في `local` أو عند `HAKEEM_SEED_DEMO=true`. `EnsureClinicServiceOfferingsSeeder` يربط الخدمات بالعيادات القائمة حتى لا تبقى صفحات `/services/{slug}` فارغة.

---

## ما تم في 23 سبتمبر 2026

| المهمة | الحالة |
| --- | --- |
| إصلاح 500: `ServiceType::isDoctorLed()` لصفحات `/services/{slug}` | في الكود؛ يحتاج `git pull` على السيرفر |
| Seeders أول إصدار + `HAKEEM_SEED_DEMO` + `EnsureClinicServiceOfferingsSeeder` | في الكود |
| Docker/`setup.sh --resetup` لأول إصدار وإعادة البناء | في الكود |
| ربط شات الموقع بـ Hermes عبر `HERMES_CHAT_URL` (OpenAI `/v1/chat/completions`) | مكتمل في الكود؛ ينتظر عنوان HTTPS العام من الوكيل |
| واجهة الصفحة الرئيسية للسوق + مسارات الاكتشاف العامة | مرفوع (`e28de1b`) |
| إصلاح 500: `Specialty::doctors()` وأيقونات الخدمات | مرفوع (`8cf35bd`) |
| Mobile API / CMS / السجل / deeplinks | مرفوع (`5d7c4b0`) |

---

## ما تم في 19 سبتمبر 2026

| المهمة | الحالة |
| --- | --- |
| خلفية الصفحة الرئيسية: تدرج أزرق فاتح ناعم (بدون برتقالي/أخضر) | مكتمل |
| تحكّم كامل بالفواتير من `/admin/billing`: حالات الدفع، نوع الاشتراك، تفاصيل الدفع، إرسال فاتورة وتذكير | مكتمل |
| صفحات الموقع من اللوحة (`/admin/site-pages`): تعديل/إضافة محتوى + سيو + خريطة الموقع | مكتمل |
| محرّر مقالات غني (خطوط/عناوين/محاذاة) بدل textarea عادي | مكتمل |
| تحصيل بوابة الدفع | غير مفعّل (يدوي من اللوحة فقط) |

---

## ما تم في 18 سبتمبر 2026

| المهمة | الحالة |
| --- | --- |
| محرّك المواعيد: ساعات الفرع/الطبيب، التعارض، واجهة slots | مكتمل |
| Mobile API: `POST /bookings` + `GET .../slots` | مكتمل |
| تذكير الموعد المجدول | مكتمل |
| المكتبة الطبية (أدمن + موقع) | مكتمل |
| تخصص×مدينة، إمكانية الوصول، بانر الكوكيز | مكتمل |
| OTP المريض (موقع + API + أسماء `/auth/otp`) | مكتمل (كان موجوداً؛ وُثّق وأُضيفت الأسماء البديلة) |
| إتمام طلب المختبر وتتبع النتيجة | مكتمل مسبقاً |
| السجل الطبي والروشتة وPDF | مكتمل مسبقاً |
| صفحات خدمة×مدينة في الموقع وSitemap | مكتمل |
| تفاصيل طبيب/عيادة + دبابيس الخريطة في الـ API | مكتمل |
| سجل المريض وطلبات التحاليل في الـ API | مكتمل |
| عنوان الزيارة المنزلية على الحجز | مكتمل |
| غرفة فيديو بعد التأكيد (Jitsi عام، بدون اعتماد جديد) | مكتمل |
| عرض مواعيد المريض بتوقيت مصر | مكتمل |

---

## ما لم يكتمل (المتبقي عمداً أو خارج هذا الإغلاق)

### 1) المدفوعات والمحاسبة الحقيقية
- لا شحن لبوابة دفع. الحالة `paid` تُعلَّم يدوياً من `/admin/billing`. التحصيل الإلكتروني يبقى غير مدفوع حتى تُضبط Paymob/Fawry.
- فواتير الاشتراك تُدار من اللوحة (رقم فاتورة، حالة دفع، طريقة/مرجع، إرسال فاتورة وتذكير لصاحب العيادة). لا تسوية للعيادات، لا عمولة منصة محسوبة، لا تصدير محاسبي.
- كود الخصم يعمل على اشتراك العيادة؛ تطبيقه كرحلة دفع لحجز المريض مرتبط بالبوابة.

### 2) وكيل Hermes (لوحة الأدمن)
- **متروك عمداً لاحقاً**. المتوفر: لقطة `/api/v1/admin/overview` بحالة `agent.status = planned`.
- لا نموذج لغوي ولا أدوات كتابة.

### 3) وكيل Hermes (المريض)
- شات الموقع يرسل إلى حكيم ثم إلى `HERMES_CHAT_URL`. مفتاح الوكيل لا يدخل المتصفح.
- نقطة الوكيل الداخلية تعمل (`hakeem-agent` على `:8650`). الموقع الحي لا يصل إليها حتى يُكشف مسار HTTPS (مثلاً `/hermes-chat/v1/chat/completions`).
- مسار OpenAI الحالي يرجع نصاً فقط ويتجاهل `hakeem.customer_token` — الزائر قد يُسأل كلمة المرور مرة أخرى حتى يُبنى المُحوّل.
- لا إنشاء حجز مباشر من المحادثة (توجيه لصفحة الحجز). لا واتساب بعد.

### 4) تطبيق الهاتف الأصلي
لا يوجد مشروع iOS/Android/React Native في هذا المستودع. الـ API يكفي لشريحة أولى (دخول، بحث، تفاصيل، slots، إنشاء حجز، سجل، تحاليل، خريطة، OTP، صندوق). المتبقي: UI أصلي، تخزين التوكن، FCM في الخلفية، متاجر التطبيقات.

### 5) إرسال فعلي للتنبيهات الخارجية
- Push/SMS/واتساب/بريد: لوج إذا غابت المفاتيح. التذكير المجدول يمر عبر نفس الموزّع. الجرس الحالي للمتصفح المفتوح وليس إشعار نظام iOS/Android.

### 6) صلاحيات أدق من الدور
- الصلاحيات مربوطة بالدور وليست مصفوفة لكل موظف. يكفي للمرحلة الحالية.

### 7) السجل الطبي عبر العيادات (مرحلة 5 المتبقية)
- السجل والروشتة والنتائج للمريض وعيادته موجودان. لا طلب موافقة عبر العيادات ولا سجل «من اطّلع على ملفي» خارج العيادة المُصدِرة.

### 8) فيديو احترافي
- الغرفة الحالية Jitsi عامة مرتبطة برمز الحجز. لا غرفة مُدارة (LiveKit/Twilio) ولا تسجيل جلسة.

### 9) أخرى قائمة
- لا بادئات `/ar`/`/en` (قرار قائم).
- `APP_TIMEZONE` يبقى UTC (قرار قائم).
- تقييمات حقيقية فقط بعد اكتمال زيارة — لا تقييمات وهمية في السيدرز.
- لا تغيير لاعتمادات Composer/NPM بدون موافقة.

---

## ملخص تنفيذي

| المجال | النسبة التقريبية | ملاحظة |
| --- | --- | --- |
| كتالوج وبحث وSEO وموقع عام | ~99% | صفحات الموقع قابلة للتحرير من اللوحة؛ ينقص SEO متقدم (خدمة×حي) عند وجود مخزون كافٍ |
| حجوزات العيادة + المريض ويب | ~95% | ينقص تحصيل البوابة وغرفة فيديو مُدارة |
| لوحة الأدمن التشغيلية | ~90% | فواتير الاشتراك من اللوحة؛ ينقص تحصيل بوابة ووكيل أدمن |
| حضور الموظفين | ~85% | شفت واحد مفتوح؛ لا سياسات تأخير/إجازات |
| إشعارات | ~75% | صندوق + جرس + تذكير مجدول؛ الإرسال الخارجي يعتمد على المفاتيح |
| Mobile API | ~85% | يكفي لشريحة التطبيق؛ ينقص رفع صور وموافقة عبر العيادات |
| تطبيق الهاتف | 0% | يُبنى على الـ API |
| Hermes أدمن | 5% | لقطة JSON فقط |
| مدفوعات | ~15% | أوضاع دفع وإعدادات؛ لا تحصيل |

---

## توقّع الخطوة التالية (ترتيب مقترح)

0. **رفع النسخة المحلية المتبقية إلى GitHub ثم السيرفر** حتى يطابق الحيّ ما في `PLATFORM_STATUS` (مكتبة، CMS، سجل، deeplinks، لوحة عيادة كاملة). ثم `git pull` + `npm ci && npm run build` + `php artisan migrate` + مسح الكاش.
1. **تشغيل شات Hermes على الحيّ**: الوكيل يكشف `:8650` خلف HTTPS، ثم تُملأ `HERMES_CHAT_URL` و`HERMES_CHAT_KEY` و`HERMES_CHAT_MODEL=hakeem-agent` على `/var/hakeem`.
2. **تطبيق الهاتف (شريحة React Native)** على الـ API الحالي: دخول، بحث، مواعيدي، صندوق، FCM.
3. **بوابة الدفع** بعد ثبات مسار الحجز (لا تُشحن الآن).
4. **موافقة السجل عبر العيادات** إن لزم التشغيل متعدد العيادات.
5. **وكيل أدمن** بعد استقرار اللوحة؛ يستهلك `/api/v1/admin/overview`.
6. **غرفة فيديو مُدارة** إن لم يكفِ Jitsi العام للعيادات التجريبية.

ما لا يُنصح به كخطوة تالية: بناء LLM لأدمن قبل التطبيق أو أول تحصيل، أو محاسبة كاملة قبل أول دفعة حقيقية.

---

## أين تُضبط الأشياء

- هوية المنصة (شعار، favicon، وصف، سوشيال، روابط التطبيق): `/admin/seo/site`
- محتوى الصفحات العامة والسيو: `/admin/site-pages`
- فواتير اشتراك العيادات: `/admin/billing`
- SMTP / Firebase / SMS / واتساب: `/admin/system`
- قنوات الإشعارات: `/admin/notification-settings`
- المستخدمون والعيادات والفواتير والخريطة والحضور والأخطاء: مجموعة «العملاء والعيادات» في الشريط الجانبي
