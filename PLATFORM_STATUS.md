# حالة منصة حكيم — ما اكتمل وما تبقّى

آخر تحديث: 24 سبتمبر 2026

حكيم سوق رعاية صحية لمصر فقط، عربي أولاً، مبني على Laravel. الهاتف هو المعرّف الأساسي للمريض. حسابات فريق المنصة تدخل من `/admin/login`. لا توجد بادئات `/ar` أو `/en` في الروابط؛ اللغة من الجلسة وRTL يتبعها. التوقيت المخزَّن يبقى UTC؛ العرض للمريض بتوقيت مصر (`Africa/Cairo`).

---

## حسابات التجربة (محلي)

| الدور | الدخول | المعرّف | كلمة المرور |
| --- | --- | --- | --- |
| مدير المنصة | `/admin/login` | `admin@hakeem.test` أو `01000000000` | `password` |
| عيادة | `/login` | `01111111111` | `password` |

واتساب الدعم الافتراضي في السيدرز: `01000000001` (منفصل عن رقم الأدمن).

---

## ما اكتمل ويعمل

### الموقع العام
- الصفحة الرئيسية (ثيم v2 + برتقالي أغمق)، الأطباء، العيادات، العروض، المختبرات، التخصصات، الخدمات، المدن، المكتبة الطبية، الصفحات التعريفية — محتواها قابل للتعديل من `/admin/site-pages` مع سيو وخريطة موقع.
- بحث متعدد الكلمات مع فلتر المحافظة والمدينة؛ محرك البحث في الـ hero على الموبايل بدون زووم/إزاحة (نتائج عائمة، خط 16px).
- هيكل موبايل شبيه بالتطبيق: viewport ثابت، قفل تمرير عند الشيتات، شريط سفلي مع safe-area.
- صفحات تخصص×مدينة/محافظة، وخدمة×مدينة/محافظة، و`/home-care` و`/teleconsultation`.
- خريطة Leaflet لفروع `/clinics`، وخريطة الفروع ذات الإحداثيات في `/admin/map`.
- حجز موعد مع slots، مدة الخدمة، طرق دفع حسب الخدمة∩العيادة∩المنصة، اختيار يوم (شرائح 14 يوماً)، عنوان زيارة منزلية عند الحاجة.
- قائمة المواعيد بتوقيت مصر، إلغاء عبر `BookingManager::transition`، تقييم بعد زيارة مكتملة فقط.
- جلسة فيديو: صفحة `/appointments/{id}/video` (Jitsi عام بعد تأكيد العيادة).
- ملف المريض، OTP، الولاء والإحالة، التأمين، السجل الطبي، سلة التحاليل، جرس التنبيهات.
- وكيل الموقع للمريض باسم **حكيم** (شات كامل الشاشة على الموبايل؛ بدون شحن بوابة دفع).

### لوحة تحكم المنصة
- بيانات مرجعية كاملة بما فيها مدة الخدمة الافتراضية وطرق الدفع لكل نوع خدمة.
- المستخدمون والعيادات والأطباء والفواتير والخريطة والحضور والتحليلات (مشاهدات الصفحات تُسجَّل) والتقييمات والتذاكر والتدقيق.
- صلاحيات حسب الدور. وكيل أدمن **مؤجَّل**: `GET /api/v1/admin/overview` فقط.

### لوحة العيادة
- ملف، فروع، أطباء، خدمات (مدة من القائمة 15/30/45/60/120)، عروض، تحاليل، طابور، حضور، فيديو.

### واجهات البرمجة
- عام + جوال Sanctum + أدمن overview كما في السابق.

### إشعارات
- صندوق داخل التطبيق + تذكير مجدول. القنوات الخارجية لوج إن نقصت المفاتيح.

---

## نشر أول إصدار / إعادة بناء (Docker)

```bash
cd /var/hakeem
git pull
./setup.sh --rebuild --domain eg.hakeem.com.sa --url https://eg.hakeem.com.sa
```

 `--resetup` يمسح قاعدة البيانات — للاستخدام أول مرة فقط أو عند إعادة البناء الكاملة.

بدون Docker بعد `git pull`:

```bash
composer install --no-dev --optimize-autoloader
npm ci && npm run build
php artisan migrate --force
php artisan optimize:clear
```

---

## ما تم في 24 سبتمبر 2026

| المهمة | الحالة |
| --- | --- |
| غلاف موبايل ثابت (بحث + شات بدون إزاحة/زووم) | مكتمل |
| إعادة تسمية وكيل الموقع إلى حكيم / Hakeem | مكتمل |
| إصلاح توجيه ضيوف `/admin` إلى `/admin/login` | مكتمل |
| تسجيل مشاهدات الصفحات للعامة (`RecordPageView`) | مكتمل |
| حفظ رمز الإحالة في الجلسة عند `/register?ref=` | مكتمل |
| فصل رقم واتساب الدعم عن رقم الأدمن في السيدرز | مكتمل |
| إعدادات الموقع الكاملة (شعار، واتساب، سوشيال، متاجر، شات) | مكتمل |
| حذف/تعديل بيانات السيدرز من الأدمن (مستخدمون، خدمات، عيادات، أطباء) | مكتمل |
| إخفاء بيانات الدخول التجريبية خارج local | مكتمل |
| إيقاف دردشة حكيم على الويب و/أو الموبايل + شريط سفلي بديل | مكتمل |
| غلاف موبايل جاهز لـ WebView قبل التطبيق الأصلي | مكتمل |
| إصلاح وكيل ماهر: مواعيد بدون IDs + حجز بالـ slug + نماذج شرطية | مكتمل |
| نسيان كلمة المرور: 422 عند رقم غير مسجّل (واجهة الوكيل فقط) | مكتمل |
| سجل محادثات الوكيل في DB + لوحة أدمن + احتفاظ 24س/90 يوم | مكتمل |
| trustProxies + تذكير APP_DEBUG=false للإنتاج | مكتمل |

---

## ما تم سابقاً (ملخص)

- تدرج الصفحة الرئيسية، فواتير الاشتراك، CMS صفحات الموقع، محرّر مقالات غني
- محرّك المواعيد، Mobile API، المكتبة، OTP، التحاليل، السجل، فيديو Jitsi
- طرق دفع ومدة لكل نوع خدمة، اختيار يوم الحجز
- Docker/`setup.sh`، Hermes chat URL، كتالوج أول إصدار

---

## ما لم يكتمل (متبقٍ عمداً — خارج إغلاق الكود الحالي)

### 1) بوابة الدفع الحقيقية
لا شحن Paymob/Fawry. الحالة `paid` يدوية من اللوحة. كود الخصم على اشتراك العيادة فقط حتى تُفعَّل البوابة.

### 2) وكيل أدمن (LLM)
متروك عمداً. لقطة `agent.status = planned` فقط.

### 3) وكيل المريض المتقدم
يحتاج كشف HTTPS لـ Hermes على السيرفر، محوّل `customer_token`، حجز مباشر من المحادثة، واتساب.

### 4) تطبيق الهاتف الأصلي
لا مشروع RN في المستودع. الـ API جاهز؛ الواجهة الويب محسّنة للموبايل/WebView كجسر أول قبل RN.

### 5) إرسال خارجي للتنبيهات
يحتاج مفاتيح SMTP/FCM/SMS/واتساب في `/admin/system`.

### 6) موافقة السجل عبر العيادات
السجل داخل العيادة المُصدِرة موجود. لا طلب موافقة بين العيادات ولا سجل اطّلاع عبر العيادات.

### 7) فيديو مُدار
Jitsi عام يكفي للتجربة. LiveKit/Twilio لاحقاً.

### 8) قرارات قائمة
لا `/ar`/`/en`، `APP_TIMEZONE=UTC`، لا تقييمات وهمية، لا اعتمادات جديدة بدون موافقة.

---

## ملخص تنفيذي

| المجال | النسبة | ملاحظة |
| --- | --- | --- |
| كتالوج وبحث وSEO وموقع | ~99% | إعدادات هوية + موبايل WebView |
| حجوزات ويب | ~95% | ينقص تحصيل البوابة |
| لوحة الأدمن | ~96% | حذف كتالوج + إعدادات كاملة |
| إشعارات | ~75% | يعتمد على المفاتيح |
| Mobile API | ~85% | جاهز لشريحة التطبيق |
| تطبيق أصلي | 0% | WebView جاهز كجسر |
| مدفوعات | ~20% | أوضاع + لكل خدمة؛ لا تحصيل |

---

## الخطوة التالية المقترحة

1. رفع النسخة بدون بيانات السيدرز التجريبية (احذف من الأدمن أو `HAKEEM_SEED_DEMO=false`).
2. ضبط `/admin/seo/site`: واتساب، شعار، سوشيال، روابط المتاجر، إيقاف الشات إن لزم.
3. `git pull` على السيرفر + rebuild/migrate.
4. كشف شات Hermes خلف HTTPS وملء `HERMES_CHAT_*`.
5. شريحة تطبيق (RN/WebView) على الـ API والصفحات الحالية.
6. بوابة الدفع بعد ثبات الحجز.

---

## أين تُضبط الأشياء

- هوية المنصة / واتساب / سوشيال / متاجر / دردشة حكيم: `/admin/seo/site`
- صفحات الموقع: `/admin/site-pages`
- فواتير الاشتراك: `/admin/billing`
- SMTP / Firebase / SMS / واتساب: `/admin/system`
- قنوات الإشعارات: `/admin/notification-settings`
- أنواع الخدمات (مدة + طرق دفع + حذف): `/admin/service-types`
- المستخدمون / العيادات / الأطباء: حذف وتعديل من صفحات التفاصيل والقوائم
